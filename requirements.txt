# ── Core ──────────────────────────────────────────
gradio>=4.0.0           # HF Spaces UI framework
requests>=2.31.0        # HTTP calls (arXiv API + HF scraping + SendGrid)
beautifulsoup4>=4.12.0  # HTML parsing for HF Papers scraper
lxml>=4.9.0             # Faster XML/HTML parser for BS4

# ── LLM ───────────────────────────────────────────
huggingface_hub>=0.20.0 # Mistral 7B via HF Inference API

# ── Email ─────────────────────────────────────────
jinja2>=3.1.0           # HTML email templating
# SendGrid uses requests (already above) — no extra SDK needed

# ── Scheduling ────────────────────────────────────
apscheduler>=3.10.0     # Daily job scheduler

# ── Utilities ─────────────────────────────────────
python-dotenv>=1.0.0    # Load .env secrets locally
